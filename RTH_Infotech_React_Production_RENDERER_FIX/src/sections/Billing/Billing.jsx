import { useLayoutEffect, useRef } from "react";
import { billingConfig } from "@/data/billing";
import SectionHeading from "@/components/common/SectionHeading";
import { mountBillingAnimation } from "@/animations/billingAnimations";
export default function Billing(){const ref=useRef(null);useLayoutEffect(()=>mountBillingAnimation(ref.current),[]);const i=billingConfig.invoice;return <section ref={ref} id="billing" className="scene billing-section"><div className="section-inner wide"><SectionHeading eyebrow="Flagship System"><h2 className="display-l">AUTOMATIC BILL<br/>GENERATION</h2></SectionHeading><div className="billing-layout"><div className="invoice-card glass-card"><p className="mono accent-text">RTH BILL AI</p><small>{i.number}</small><span>{i.company}</span><dl><div><dt>Subtotal</dt><dd>{i.subtotal}</dd></div><div><dt>GST 18%</dt><dd>{i.tax}</dd></div><div className="total"><dt>Total</dt><dd>{i.total}</dd></div></dl></div><div className="status-stack"><p className="mono">EXECUTION SEQUENCE</p>{billingConfig.steps.map((s,i)=><div className="billing-step" key={s}><i className="status-check">✓</i><span className="mono">{String(i+1).padStart(2,'0')}</span><b>{s}</b></div>)}</div></div></div></section>}
